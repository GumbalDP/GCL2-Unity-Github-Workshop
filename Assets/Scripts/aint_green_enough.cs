using UnityEngine;

public class aint_green_enough : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("if green is your power what are you without it");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
