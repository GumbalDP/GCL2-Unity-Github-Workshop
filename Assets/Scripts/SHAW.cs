using UnityEngine;

public class SHAW : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("SHAW, HEGALE, ADINO, GARAMA, GITGUD");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
