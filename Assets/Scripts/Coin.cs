using UnityEngine;

public class Coin : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("added 1 coin lol");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
